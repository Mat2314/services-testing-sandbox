# Services testing sandbox

The repository holds a docker-compose.yml file that contains multiple services to test throuhg like in the sandbox.
For example:

- FastAPI
- Django
- Angular
- Redis
- PostgreSQL
- Prometheus
- Grafana
- etc
